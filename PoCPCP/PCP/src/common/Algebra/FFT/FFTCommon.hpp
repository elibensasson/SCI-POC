#define MATAN_FFT_LOWER_BOUND (6)
#define MATAN_IFFT_LOWER_BOUND (6)
#define USE_MATAN (true) //determines whether to use Matan's FFT implementation (currently only working for field size 64), or the older FFT implementation

